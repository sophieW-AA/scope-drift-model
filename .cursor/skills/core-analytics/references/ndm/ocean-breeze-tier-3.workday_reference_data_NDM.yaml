# Analytical Data Model - ocean-breeze-tier-3.workday_reference_data
# Narrow Data Model (NDM) - Version 2
# NDM scope: keys (_id columns), primary keys, and foreign keys only.
# Generated: 2026-04-10
# Source: dataset-model-ADM/ocean-breeze-tier-3.workday_reference_data_ADM_V2.yaml
#
# -- Join rules ----------------------------------------------------------------
# - space_id is an implicit second join condition wherever the grain includes it.
# - When joining two tables within the dataset, space_id is not strictly
#   required - the primary key is sufficient.
# - When crossing into external datasets, use XXX_original + space_id.
# - If space_id is absent in a table, filter by space_id = 1 in the table
#   that does carry space_id as a safety measure.
#
# -- FK cardinality conventions ------------------------------------------------
# many_to_one          : many rows here -> same parent row (standard FK)
# one_to_one           : at most one row here per referenced parent row
# optional_many_to_one : FK is nullable / zero-or-one participation
# [PROPOSED]           : inferred - validate before use
# -----------------------------------------------------------------------------

entities:

  ######################## JOURNAL ########################

  - name: journal_cost_center
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.journal_cost_center
    description: "One row per journal_cost_center."
    grain: space_id, journal_id
    primary_key: space_id, journal_id
    columns:
      - name: space_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: space
            column: space_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: journal_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: journal
            column: journal_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: cost_center_id
        type: String

  - name: journal_cost_center_historical
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.journal_cost_center_historical
    description: "[PROPOSED] One row per journal_cost_center_historical."
    grain: space_id, journal_id
    primary_key: space_id, journal_id
    columns:
      - name: space_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: space
            column: space_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: journal_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: journal
            column: journal_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: cost_center_id
        type: String

  - name: journal_owner
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.journal_owner
    description: "[PROPOSED] One row per journal_owner."
    grain: journal_id, space_id
    primary_key: journal_id, space_id
    columns:
      - name: journal_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: journal
            column: journal_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: space_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: space
            column: space_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: journal_team_id
        type: String
        foreign_keys:
          - scope: internal
            entity: journal_team
            column: journal_team_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: frontiers_role_id
        type: String
        foreign_keys:
          - scope: internal
            entity: frontiers_role
            column: frontiersrole_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: portfolio_mgr_id
        type: String
        foreign_keys:
          - scope: internal
            entity: employee
            column: employee_id
            cardinality: many_to_one
            relationship: "is managed references"

  - name: journal_owner_historical
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.journal_owner_historical
    description: "[PROPOSED] One row per journal_owner_historical."
    grain: journal_id, space_id
    primary_key: journal_id, space_id
    columns:
      - name: journal_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: journal
            column: journal_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: space_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: space
            column: space_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: journal_team_id
        type: String
        foreign_keys:
          - scope: internal
            entity: journal_team
            column: journal_team_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: frontiers_role_id
        type: String
        foreign_keys:
          - scope: internal
            entity: frontiers_role
            column: frontiersrole_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: portfolio_mgr_id
        type: String
        foreign_keys:
          - scope: internal
            entity: employee
            column: employee_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"

  - name: journal_segment
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.journal_segment
    description: "[PROPOSED] One row per journal_segment."
    grain: journal_segment_id
    primary_key: journal_segment_id
    columns:
      - name: journal_segment_id
        type: String

  - name: journal_segment_historical
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.journal_segment_historical
    description: "[PROPOSED] One row per journal_segment_historical."
    grain: journal_segment_id
    primary_key: journal_segment_id
    columns:
      - name: journal_segment_id
        type: String
        foreign_keys:
          - scope: internal
            entity: journal_segment
            column: journal_segment_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"

  - name: journal_team
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.journal_team
    description: "[PROPOSED] One row per journal_team."
    grain: journal_team_id
    primary_key: journal_team_id
    columns:
      - name: journal_team_id
        type: String

  - name: journal_team_historical
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.journal_team_historical
    description: "[PROPOSED] One row per journal_team_historical."
    grain: journal_team_id
    primary_key: journal_team_id
    columns:
      - name: journal_team_id
        type: String
        foreign_keys:
          - scope: internal
            entity: journal_team
            column: journal_team_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"

  - name: journal_workday
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.journal_workday
    description: "[PROPOSED] One row per journal_workday."
    grain: journal_id, workday_segment_id
    primary_key: journal_id, workday_segment_id
    columns:
      - name: journal_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: journal
            column: journal_id
            cardinality: many_to_one
            relationship: " it refers to"
      - name: workday_segment_id
        type: String
      - name: space_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: space
            column: space_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: program_id
        type: String
        foreign_keys:
          - scope: internal
            entity: program_name
            column: program_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: journal_segment_id
        type: String

  - name: journal_workday_historical
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.journal_workday_historical
    description: "[PROPOSED] One row per journal_workday_historical."
    grain: journal_id, workday_journal_id
    primary_key: journal_id, workday_journal_id
    columns:
      - name: journal_id
        type: Integer
        foreign_keys:
          - scope: internal
            entity: journal_workday
            column: journal_id
            cardinality: many_to_one
            relationship: "it refers to"
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: journal
            column: journal_id
            cardinality: many_to_one
            relationship: "it refers to"
      - name: workday_journal_id
        type: String
      - name: space_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: space
            column: space_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: program_id
        type: String
        foreign_keys:
          - scope: internal
            entity: program_name
            column: program_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: workday_segment_id
        type: String
      - name: journal_segment_id
        type: String

  ######################## EMPLOYEE ########################

  - name: employee
    domain: EMPLOYEE
    source_table: ocean-breeze-tier-3.workday_reference_data.employee
    description: "[PROPOSED] One row per employee."
    grain: employee_id
    primary_key: employee_id
    columns:
      - name: employee_id
        type: String
      - name: frontiers_role_id
        type: String
        foreign_keys:
          - scope: internal
            entity: frontiers_role
            column: frontiersrole_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"
      - name: cost_center_id
        type: String

  - name: employee_historical
    domain: EMPLOYEE
    source_table: ocean-breeze-tier-3.workday_reference_data.employee_historical
    description: "[PROPOSED] One row per employee_historical."
    grain: employee_id, cost_center_id
    primary_key: employee_id, cost_center_id
    columns:
      - name: employee_id
        type: String
        foreign_keys:
          - scope: internal
            entity: employee
            column: employee_id
            cardinality: optional_many_to_one
            relationship: "[PROPOSED] references"
      - name: cost_center_id
        type: String
      - name: frontiers_role_id
        type: String

  ######################## UNKNOWN ########################

  - name: frontiers_role
    domain: EMPLOYEE
    source_table: ocean-breeze-tier-3.workday_reference_data.frontiers_role
    description: "[PROPOSED] One row per frontiers_role."
    grain: frontiersrole_id
    primary_key: frontiersrole_id
    columns:
      - name: frontiersrole_id
        type: String

  - name: frontiers_role_historical
    domain: EMPLOYEE
    source_table: ocean-breeze-tier-3.workday_reference_data.frontiers_role_historical
    description: "[PROPOSED] One row per frontiers_role_historical."
    grain: frontiersrole_id
    primary_key: frontiersrole_id
    columns:
      - name: frontiersrole_id
        type: String
        foreign_keys:
          - scope: internal
            entity: frontiers_role
            column: frontiersrole_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"

  - name: program_name
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.program_name
    description: "[PROPOSED] One row per program_name."
    grain: program_id
    primary_key: program_id
    columns:
      - name: program_id
        type: String

  - name: program_name_historical
    domain: JOURNAL
    source_table: ocean-breeze-tier-3.workday_reference_data.program_name_historical
    description: "[PROPOSED] One row per program_name_historical."
    grain: program_id
    primary_key: program_id
    columns:
      - name: program_id
        type: String
        foreign_keys:
          - scope: internal
            entity: program_name
            column: program_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"

  - name: project_role
    domain: EMPLOYEE
    source_table: ocean-breeze-tier-3.workday_reference_data.project_role
    description: "[PROPOSED] One row per project_role."
    grain: project_role_id
    primary_key: project_role_id
    columns:
      - name: project_role_id
        type: String

  - name: project_role_historical
    domain: EMPLOYEE
    source_table: ocean-breeze-tier-3.workday_reference_data.project_role_historical
    description: "[PROPOSED] One row per project_role_historical."
    grain: project_role_id
    primary_key: project_role_id
    columns:
      - name: project_role_id
        type: String
        foreign_keys:
          - scope: internal
            entity: project_role
            column: project_role_id
            cardinality: many_to_one
            relationship: "[PROPOSED] references"

