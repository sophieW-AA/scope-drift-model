# Analytical Data Model — ocean-breeze-tier-2.production_forum
# Narrow Data Model (NDM) — Version 1
# NDM scope: keys (_id columns), primary keys, and foreign keys only.
# Generated: 2026-04-03
# Source: ocean-breeze-tier-2.production_forum_ADM_V1.yaml
#
# ── Join rules ────────────────────────────────────────────────────────────────
# • space_id is an implicit second join condition wherever the grain includes it.
# • When joining two tables within the dataset, space_id is not strictly
# required — the primary key is sufficient.
# • When crossing into external datasets, use XXX_original + space_id.
# • If space_id is absent in a table, filter by space_id = 1 in the table
# that does carry space_id as a safety measure.
#
# ── FK cardinality conventions ────────────────────────────────────────────────
# many_to_one         : many rows here → same parent row (standard FK)
# one_to_one          : at most one row here per referenced parent row
# optional_many_to_one: FK is nullable / zero-or-one participation
# [PROPOSED]          : inferred — validate before use
# ─────────────────────────────────────────────────────────────────────────────

entities:

  ######################## ARTICLE ########################

  - name: article
    domain: ARTICLE
    source_table: ocean-breeze-tier-2.production_forum.article
    description: "One row per article. Article of the production office."
    grain: id
    primary_key: id
    columns:
      - name: id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: article
            column: article_id_original
            cardinality: one_to_one
            relationship: "it refers to"
      - name: space_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: space
            column: space_id
            cardinality: many_to_one
            relationship: "is part of"


  - name: author
    domain: ARTICLE
    source_table: ocean-breeze-tier-2.production_forum.author
    description: "One row per author. Author of the article."
    grain: id
    primary_key: id
    columns:
      - name: id
        type: Integer
      - name: article_id
        type: Integer
        foreign_keys:
          - scope: internal
            entity: article
            column: id
            cardinality: many_to_one
            relationship: "is the author of"
      - name: user_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: user_metrics
            column: user_id
            cardinality: many_to_one
            relationship: "refers to user"
      - name: space_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: space
            column: space_id
            cardinality: many_to_one
            relationship: "is part of"
      - name: institution_id
        type: Integer
        foreign_keys:
          - scope: internal
            entity: institution
            column: id
            cardinality: many_to_one
            relationship: "is part of"

  - name: content_type
    domain: ARTICLE
    source_table: ocean-breeze-tier-2.production_forum.content_type
    description: "Content_type of article elements"
    grain: id
    primary_key: id
    columns:
      - name: id
        type: Integer

  - name: file
    domain: ARTICLE
    source_table: ocean-breeze-tier-2.production_forum.file
    description: "One row per file. Article related files."
    grain: file_id, space_id
    primary_key: file_id, space_id
    columns:
      - name: article_id
        type: Integer
        foreign_keys:
          - scope: internal
            entity: article
            column: id
            cardinality: many_to_one
            relationship: "is part of"
      - name: file_id
        type: String
      - name: content_type_id
        type: Integer
      - name: space_id
        type: Integer

  - name: author_institution
    domain: ARTICLE
    source_table: ocean-breeze-tier-2.production_forum.author_institution
    description: " One row per author_institution. Bridge table linking authors to institutions."
    grain: id
    primary_key: id
    columns:
      - name: id
        type: Integer
      - name: author_id
        type: Integer
        foreign_keys:
          - scope: internal
            entity: author
            column: id
            cardinality: many_to_one
            relationship: "links author to institution"
      - name: institution_id
        type: Integer
        foreign_keys:
          - scope: internal
            entity: institution
            column: id
            cardinality: many_to_one
            relationship: "links to institution"
      - name: space_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: space
            column: space_id
            cardinality: many_to_one
            relationship: "is part of"


  - name: institution
    domain: ORGANIZATION
    source_table: ocean-breeze-tier-2.production_forum.institution
    description: "One row per institution. Institutions of the authors."
    grain: id
    primary_key: id
    columns:
      - name: id
        type: Integer
      - name: rosst_id
        type: String
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-2.rosst
            entity: organization
            column: OrganizationId
            cardinality: many_to_one
            relationship: "is part of"
      - name: space_id
        type: Integer
        foreign_keys:
          - scope: external
            dataset: ocean-breeze-tier-1.reporting_data_mart
            entity: space
            column: space_id
            cardinality: many_to_one
            relationship: "is part of"
